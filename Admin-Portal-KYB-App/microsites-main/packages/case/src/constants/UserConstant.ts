export enum GUEST_USER {
	GUEST_FIRST_NAME = "Guest",
	GUEST_LAST_NAME = "Owner",
	GUEST_EMAIL_PREFIX = "guest+",
}

export enum USER_ROLE {
	ADMIN = "admin",
	APPLICANT = "applicant",
	CUSTOMER = "customer",
}
