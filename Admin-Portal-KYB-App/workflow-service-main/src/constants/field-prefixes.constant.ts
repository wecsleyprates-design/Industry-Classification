/**
 * Field path prefixes used throughout the application
 * These constants centralize field path prefixes to avoid hardcoding strings
 */
export const FIELD_PREFIXES = {
	CASE: "case",
	FACTS: "facts",
	CUSTOM_FIELDS: "custom_fields"
} as const;
