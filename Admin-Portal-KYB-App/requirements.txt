# KYB Intelligence Hub — Python dependencies
# Install with: pip3 install -r requirements.txt

# ── Core app framework ─────────────────────────────────────────────────────────
streamlit>=1.41.0,<2.0.0

# ── Data & visualisation ───────────────────────────────────────────────────────
pandas>=2.0.0,<4.0.0
plotly>=5.0.0,<7.0.0
numpy>=1.23.0,<3.0.0

# ── AI / OpenAI ────────────────────────────────────────────────────────────────
openai>=1.0.0,<3.0.0

# ── Database (Redshift via psycopg2) ──────────────────────────────────────────
# Use psycopg2-binary — no C compiler required on most systems
psycopg2-binary>=2.9.0,<3.0.0

# ── File export ────────────────────────────────────────────────────────────────
openpyxl>=3.1.0,<4.0.0       # required for .xlsx download buttons
tabulate>=0.9.0               # required for df.to_markdown() in AI results

# ── HTTP / networking (used by openai and streamlit) ──────────────────────────
httpx>=0.23.0,<1.0.0
requests>=2.27.0,<3.0.0

# ── Utilities (used internally) ────────────────────────────────────────────────
python-dateutil>=2.8.0
pyarrow>=10.0.0               # required by streamlit for dataframe rendering
pillow>=9.0.0                 # required by streamlit for image handling
